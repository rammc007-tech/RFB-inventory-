{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 1,
    "sleepApplication": false,
    "useLegacyStacker": false,
    "startCommand": "set +e; echo '🚀 Starting Railway application...'; if [ -n \"$DATABASE_URL\" ]; then echo '📊 Running migrations...'; npx prisma migrate deploy 2>&1 || npx prisma db push --accept-data-loss 2>&1 || echo '⚠️ Migration warning (continuing)'; echo '🌱 Seeding database...'; npm run prisma:seed 2>&1 || echo '⚠️ Seeding warning (continuing)'; else echo '⚠️ DATABASE_URL not set - starting without database'; fi; echo '✅ Starting Next.js...'; exec next start",
    "multiRegionConfig": {
      "us-west2": {
        "numReplicas": 1
      }
    },
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
