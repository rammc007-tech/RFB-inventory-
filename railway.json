{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node scripts/switch-to-postgres.js && npx prisma generate && npx prisma migrate deploy && npm run prisma:seed && npm run build"
  },
  "deploy": {
    "startCommand": "next start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

