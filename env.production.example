# Production Environment (Railway)
# Railway will automatically use internal URL at runtime
# DO NOT commit this file - Railway sets DATABASE_URL automatically

# Railway automatically provides DATABASE_URL with internal URL
# Format: postgresql://postgres:password@postgres.railway.internal:5432/railway
# This is only available at runtime, not during build

# NextAuth Configuration (set in Railway variables)
NEXTAUTH_URL="https://your-app.up.railway.app"
NEXTAUTH_SECRET="your-production-secret-key-min-32-chars"

